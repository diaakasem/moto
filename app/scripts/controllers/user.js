'use strict';

/**
 * @ngdoc function
 * @name motoApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the motoApp
 */
angular.module('motoApp')
  .controller('UserCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
